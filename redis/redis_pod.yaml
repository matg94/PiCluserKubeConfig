apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    name: redis-pod
spec:
  containers:
  - image: redis
    name: redis
    ports:
    - containerPort: 6379
      protocol: TCP
    volumeMounts:
    - name: redis-nfs-pv
      mountPath: "/redis"
  volumes:
  - name: redis-nfs-pv
    persistentVolumeClaim:
      claimName: redis-nfs-pvc
